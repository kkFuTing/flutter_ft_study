# 打代码快捷键
stf StatefulWidget
stl StatelessWidget

// 选中ListView 右键-refactor-extract-widget (-to-new-file)

创建命令
flutter create -org com.dongnaoedu -i swift -a kotlin flutter_demo
-org参数：指定Android包名（如com.dongnaoedu）
-i参数：指定iOS开发语言（如swift）
-a参数：指定Android开发语言（如kotlin）